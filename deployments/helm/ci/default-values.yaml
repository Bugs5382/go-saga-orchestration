# Lint/render fixture: exercises the postgres store, connection secret, ingress,
# and the dedicated cron pod paths.
store:
  type: postgres
connectionSecret: gosaga-conn
ingress:
  enabled: true
  className: nginx
  host: saga.example.com
cron:
  dedicated: true
