# Lint/render fixture: exercises the postgres store, connection secret, and ingress paths.
store:
  type: postgres
connectionSecret: gosaga-conn
ingress:
  enabled: true
  className: nginx
  host: saga.example.com
